# Emergency Kit Scanner - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                    (Next.js / React / TypeScript)              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EMERGENCY KIT PAGE                           │
│                /emergency/kit/page.tsx                          │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Disaster Type Selection                                │    │
│  │ • Tsunami  • Earthquake  • Hurricane                   │    │
│  │ • Flood    • Wildfire                                  │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Component
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              EMERGENCY KIT SCANNER COMPONENT                    │
│          components/emergency/EmergencyKitScanner.tsx           │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Camera Interface                                       │    │
│  │ • Live video feed                                      │    │
│  │ • Capture controls                                     │    │
│  │ • Base64 encoding                                      │    │
│  └────────────────────────────────────────────────────────┘    │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Checklist Display                                      │    │
│  │ • Visual checkmarks                                    │    │
│  │ • Confidence scores                                    │    │
│  │ • Progress tracking                                    │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ POST /api/detect-kit-items
                              │ {image: base64, disasterType: string}
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      AI SERVER                                  │
│              (Python / Flask / Port 5000)                       │
│                     credio-ai-server/app.py                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Image Processing
                              ▼
              ┌───────────────────────────────┐
              │    Detection Strategy         │
              └───────────────────────────────┘
                      │               │
            Primary   │               │   Fallback
                      ▼               ▼
         ┌─────────────────┐   ┌──────────────────┐
         │  YOLO v8        │   │  GPT-4 Vision    │
         │  (Local)        │   │  (OpenAI API)    │
         │  • Fast         │   │  • Accurate      │
         │  • Offline      │   │  • Cloud-based   │
         │  • 100-500ms    │   │  • 2-5 seconds   │
         └─────────────────┘   └──────────────────┘
                      │               │
                      └───────┬───────┘
                              │
                              ▼
                ┌──────────────────────────┐
                │   Object Recognition     │
                │   (80+ COCO classes)     │
                └──────────────────────────┘
                              │
                              ▼
                ┌──────────────────────────┐
                │ Match with Disaster      │
                │ Specific Checklist       │
                └──────────────────────────┘
                              │
                              │ JSON Response
                              ▼
         ┌──────────────────────────────────────┐
         │  {                                   │
         │    checklist: [                      │
         │      {name, detected, confidence}    │
         │    ],                                │
         │    detectionMethod: "yolo"|"gpt"     │
         │  }                                   │
         └──────────────────────────────────────┘
```

## Component Interactions

```
User Action Flow:
─────────────────

1. User selects disaster type
   └─> Renders EmergencyKitScanner component

2. User clicks "Start Camera"
   └─> Requests camera permissions
       └─> Activates video stream

3. User positions items in frame
   └─> Live preview displayed

4. User clicks "Scan Items"
   └─> Captures current frame
       └─> Converts to base64
           └─> Sends to AI server

5. AI Server processes image
   └─> Attempts YOLO detection
       ├─> Success: Returns results
       └─> Failure: Falls back to GPT-4
           └─> Returns results

6. Client receives results
   └─> Updates checklist UI
       └─> Shows detected items
           └─> Displays confidence scores
```

## Data Flow

```
Image Capture:
───────────────
Video Stream → Canvas → toDataURL() → Base64 String

API Request:
────────────
{
  image: "data:image/jpeg;base64,/9j/4AAQ...",
  disasterType: "tsunami"
}

AI Processing:
──────────────
Base64 → Decode → NumPy Array → YOLO/GPT → Object List

Response:
─────────
{
  success: true,
  disasterType: "tsunami",
  requiredItems: ["flashlight", "bottle", ...],
  checklist: [
    {
      name: "flashlight",
      detected: true,
      confidence: 0.95
    },
    ...
  ],
  detectionMethod: "yolo"
}
```

## Technology Stack

```
Frontend Layer:
───────────────
├─ Next.js (Framework)
├─ React (UI Library)
├─ TypeScript (Type Safety)
├─ Tailwind CSS (Styling)
└─ Shadcn/ui (Components)

AI Server Layer:
────────────────
├─ Python 3.9+ (Runtime)
├─ Flask (Web Framework)
├─ YOLOv8 (Primary Detection)
│  ├─ Ultralytics
│  ├─ PyTorch
│  └─ OpenCV
└─ GPT-4 Vision (Fallback)
   └─ OpenAI API

Integration:
────────────
├─ REST API (HTTP)
├─ JSON (Data Format)
├─ Base64 (Image Encoding)
└─ CORS (Cross-Origin)
```

## Deployment Architecture

```
Development:
────────────
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Client         │  │  Node Server    │  │  AI Server      │
│  :3000          │  │  :3001          │  │  :5000          │
│  npm run dev    │  │  npm start      │  │  python app.py  │
└─────────────────┘  └─────────────────┘  └─────────────────┘
         │                    │                     │
         └────────────────────┴─────────────────────┘
                      localhost

Production (Future):
────────────────────
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Vercel/Netlify │  │  AWS/Heroku     │  │  AWS Lambda/    │
│  (Next.js)      │  │  (Node.js API)  │  │  GCP Run        │
│                 │  │                 │  │  (Python AI)    │
└─────────────────┘  └─────────────────┘  └─────────────────┘
         │                    │                     │
         └────────────────────┴─────────────────────┘
                        Internet
```

## Security Model

```
Security Layers:
────────────────

1. Camera Access
   └─> User must explicitly grant permissions
       └─> Only activated on button click

2. API Keys
   └─> Stored in .env (not committed)
       └─> Server-side only

3. Image Handling
   └─> Processed in memory
       └─> No persistent storage
           └─> Automatic cleanup

4. CORS
   └─> Development: localhost only
       └─> Production: specific domains

5. Rate Limiting (Future)
   └─> Prevent abuse
       └─> API quota management
```

## Disaster Type Checklists

```
┌──────────────────────────────────────────────────────────────┐
│                    Disaster Types                            │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  🌊 Tsunami        ⚡ Earthquake      🌀 Hurricane          │
│  🌊 Flood          🔥 Wildfire                              │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│              Common Essential Items (All Disasters)          │
├──────────────────────────────────────────────────────────────┤
│  • Flashlight          • Water bottle      • Backpack        │
│  • Important docs      • Cell phone        • First aid       │
│  • Food supplies       • Emergency tools                     │
└──────────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
Error Scenarios:
────────────────

Camera Access Denied
└─> Display error message
    └─> Guide user to grant permissions

YOLO Detection Fails
└─> Automatic fallback to GPT-4
    └─> User notified via "detectionMethod"

GPT-4 API Error
└─> Display error message
    └─> Suggest retry

Network Error
└─> Display connection error
    └─> Check server status

Invalid Image
└─> Display format error
    └─> Prompt to recapture
```

This architecture provides a scalable, maintainable, and user-friendly emergency kit verification system!
